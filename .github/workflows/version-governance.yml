name: version-governance

on:
  pull_request:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  version-governance:
    uses: ./.github/workflows/reusable-version-governance.yml
    with:
      governance-ref: ${{ github.event.pull_request.head.sha || github.sha }}
      target-repository: ${{ github.repository }}
      target-ref: ${{ github.event.pull_request.head.sha || github.sha }}
      expected-repository: ${{ github.repository }}
      plan-path: docs/releases/plan.json
      tag: ${{ github.ref_type == 'tag' && github.ref_name || '' }}
      require_tag: ${{ github.ref_type == 'tag' }}
      github-check: false
